-- Minimal stub so snippets that `require("util.math_autochange")` don’t error.
-- If you don’t use rime-ls, leaving these as no-ops is fine.
local M = {}

function M.switch_rime_math()
  -- intentionally empty
end

function M.force_toggle_rime()
  -- intentionally empty
end

return M
